const {supabase}=require('./_supabase');const {userId}=require('./_auth');module.exports=async(req,res)=>{if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const uid=await userId(req);if(!uid)return res.status(401).json({error:'Not authenticated'});const r=await supabase.from('memories').select('tags').eq('user_id',uid);if(r.error)return res.status(500).json({error:r.error.message});const c={};(r.data||[]).forEach(x=>(x.tags||[]).forEach(t=>c[t]=(c[t]||0)+1));res.json({tags:Object.entries(c).sort((a,b)=>b[1]-a[1]).map(([name,count])=>({name,count}))})};
